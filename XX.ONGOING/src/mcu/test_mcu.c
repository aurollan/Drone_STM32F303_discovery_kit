#include <stdint.h>

void launch_test_sequence(uint32_t start_tests) {
    if (1 == start_tests) {
        // set_duty_cycle(TIM_Channel_1, 25);
        // set_duty_cycle(TIM_Channel_2, 50);
        // set_duty_cycle(TIM_Channel_3, 75);
        // set_duty_cycle(TIM_Channel_4, 100);

    }
}
